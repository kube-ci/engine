apiVersion: admissionregistration.k8s.io/v1beta1
kind: ValidatingWebhookConfiguration
metadata:
  name: admission.kubeci.kube.ci
  labels:
    app: kubeci
webhooks:
- name: workflow.admission.kubeci.kube.ci
  clientConfig:
    service:
      namespace: default
      name: kubernetes
      path: /apis/admission.kubeci.kube.ci/v1alpha1/repositories
    caBundle: ${KUBE_CA}
  rules:
  - operations:
    - CREATE
    - UPDATE
    apiGroups:
    - kubeci.kube.ci
    apiVersions:
    - "*"
    resources:
    - workflows
  failurePolicy: Fail
